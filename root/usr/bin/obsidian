#!/bin/bash

BIN=/opt/obsidian/obsidian

# Wayland check
if [ "${PIXELFLUX_WAYLAND,,}" == "true" ]; then
  WAYLAND="--ozone-platform=wayland"
fi

${BIN} \
  --no-sandbox \
  ${WAYLAND} \
  "$@" > /dev/null 2>&1
